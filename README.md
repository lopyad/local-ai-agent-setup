# 로컬 AI 에이전트 설정 가이드 (Windows 11)

이 가이드는 Ollama와 Continue.dev를 사용하여 Windows 11 환경에서 로컬 AI 에이전트를 설정하는 절차를 설명합니다. 제공된 `Modelfile`과 `.continue.config.yaml` 파일을 활용합니다.

## 1. 사전 준비 사항

- **Ollama 설치**: [ollama.com](https://ollama.com/)에서 Windows용 Ollama를 다운로드하여 설치하세요.
- **VS Code 및 Continue 설치**: VS Code를 실행하고 Extension 마켓플레이스에서 **Continue**를 설치하세요.

---

## 2. Ollama 모델 생성 (`Modelfile` 사용)

`Modelfile`은 로컬에 있는 GGUF 모델 파일을 Ollama에서 사용할 수 있도록 정의하는 설정 파일입니다.

1.  **Modelfile 수정**:
    - `Modelfile`을 열고 첫 번째 줄의 경로를 실제 GGUF 파일이 있는 경로로 수정하세요.
    - 예: `FROM "C:\Users\YourName\Downloads\gemma-4-E4B-it-Q4_K_M.gguf"`
2.  **터미널 실행**: PowerShell 또는 명령 프롬프트(CMD)를 엽니다.
3.  **모델 생성 명령 실행**: `Modelfile`이 있는 디렉토리로 이동한 후 다음 명령어를 입력합니다.
    ```powershell
    # 채팅 및 편집용 모델 생성
    ollama create google/gemma-4-e4b -f Modelfile

    # 자동 완성용 모델이 별도로 있다면 동일한 방식으로 생성하세요.
    # 예: ollama create gemma-4-e2b-it-uncensored -f AutocompleteModelfile
    ```
4.  **확인**: 모델이 정상적으로 생성되었는지 확인합니다.
    ```powershell
    ollama list
    ```

---

## 3. Continue.dev 설정 (`.continue.config.yaml` 사용)

Continue.dev가 생성된 Ollama 모델을 인식하고 사용할 수 있도록 설정 파일을 적용합니다.

1.  **설정 파일 위치 이동**:
    - Windows 탐색기 주소창에 `%USERPROFILE%\.continue`를 입력하여 이동합니다.
    - (일반적으로 `C:\Users\<사용자이름>\.continue` 경로입니다.)
2.  **config.yaml 적용**:
    - 해당 폴더에 기존 `config.yaml` (또는 `config.json`)이 있다면 백업해두세요.
    - 프로젝트 폴더의 `.continue.config.yaml` 파일 내용을 복사하여 `%USERPROFILE%\.continue\config.yaml`에 덮어쓰거나 내용을 병합합니다.
    - **주의**: `.continue.config.yaml` 파일의 `apiBase`가 `http://localhost:11434/v1`로 설정되어 있는지 확인하세요 (Ollama 기본 주소).

3.  **Continue 재시작**:
    - VS Code에서 Continue 사이드바를 열고, 모델 선택 드롭다운에서 `gemma4-e4b` (Chat용) 또는 `gemma-4-e2b` (Autocomplete용) 모델이 나타나는지 확인합니다.

---

## 4. 주요 설정 설명

### .continue.config.yaml
- **Chat/Edit 모델**: `gemma4-e4b` 모델이 채팅과 코드 수정(Edit) 역할로 지정되어 있습니다.
- **Autocomplete 모델**: `gemma-4-e2b` 모델이 코드 자동 완성 역할을 수행합니다.
- **API Base**: Ollama의 OpenAI 호환 API 서버를 통해 통신합니다.

### Modelfile
- **System Message**: AI에게 전문 소프트웨어 엔지니어 역할을 부여하여 간결하고 버그 없는 코드를 작성하도록 지시합니다.
- **Parameters**: `temperature 0.1`로 설정되어 일관성 있는 답변을 생성하며, `num_ctx 16384`로 넓은 컨텍스트 창을 제공합니다.

---

## 5. 문제 해결 (Troubleshooting)

- **경로 오류**: `Modelfile`의 `FROM` 경로에 공백이 포함되어 있다면 반드시 큰따옴표(`"`)로 감싸주세요.
- **Ollama 실행 여부**: 모델 생성이나 채팅 시 오류가 발생하면 작업 표시줄 트레이에서 Ollama가 실행 중인지 확인하세요.
- **포트 충돌**: 다른 서비스가 `11434` 포트를 사용 중인지 확인하세요.

generated by geminie-3-flash